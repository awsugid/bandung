<section class="site-section border-t border-border">
	<div class="site-container">
		<p class="eyebrow">About</p>
		<h2 class="mt-6 max-w-4xl text-4xl uppercase leading-[1.04] sm:text-6xl">
			<span class="block">From the <span class="accent comday-flow" style="animation-delay: 0s">community</span></span>
			<span class="block">run by the <span class="accent comday-flow" style="animation-delay: -5s">community</span></span>
			<span class="block">for the <span class="accent comday-flow" style="animation-delay: -10s">community</span>.</span>
		</h2>

		<div class="mt-12 grid gap-10 border-t border-border pt-10 lg:grid-cols-2 lg:gap-16">
			<p class="lead">
				AWS Community Day is a one-day cloud conference organized by AWS User
				Groups around the world, with talks shared by builders from the
				community.
			</p>

			<div>
				<ol class="flex items-center gap-3 text-sm font-bold uppercase tracking-[0.18em]" aria-label="AWS Community Day in Indonesia">
					<li class="flex items-center gap-2 text-muted-foreground">
						<span class="h-2.5 w-2.5 rounded-full border-2 border-muted-foreground" aria-hidden="true"></span>
						Jakarta
					</li>
					<li class="route h-px flex-1" aria-hidden="true"></li>
					<li class="flex items-center gap-2 text-white">
						<span class="h-2.5 w-2.5 rounded-full bg-primary shadow-[0_0_0_4px_color-mix(in_oklab,var(--primary)_25%,transparent)]" aria-hidden="true"></span>
						Bandung 2026
					</li>
				</ol>
				<p class="mt-5">
					In Indonesia it has so far been held in Jakarta. This year Bandung
					hosts it for the first time, organized by AWS User Group Bandung with
					AWS User Group Indonesia and supported by Amazon Web Services.
				</p>
			</div>
		</div>
	</div>
</section>

<style>
	/* Pad the box so the gradient reaches glyph ink past the text box (the Y's arm). */
	.accent {
		padding-inline: 0.1em;
		margin-inline: -0.1em;
		-webkit-box-decoration-break: clone;
		box-decoration-break: clone;
		-webkit-background-clip: text;
		background-clip: text;
		color: transparent;
	}

	.route {
		background: repeating-linear-gradient(90deg, var(--border) 0 6px, transparent 6px 12px);
		height: 2px;
	}
</style>
